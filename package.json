{
	"version": "0.0.0",
	"author": "letnull19a",
	"devDependencies": {
		"concurrently": "^9.1.2",
		"husky": "^9.1.7",
		"prettier": "^3.5.3",
		"rimraf": "^6.1.2"
	},
	"scripts": {
		"prepare": "husky",
		"format": "prettier --write **/src/**/*{.js,.ts,.tsx,.jsx}",
		"dev": "concurrently \"cd frontend && pnpm run dev\" \"cd server && pnpm run start:dev\"",
		"build": "pnpm run build:frontend && pnpm run build:backend && pnpm run copy:frontend",
		"build:frontend": "cd frontend && pnpm run build",
		"build:backend": "cd server && pnpm run build",
		"copy:frontend": "rimraf server/dist/frontend && mkdir -p server/dist && cp -r frontend/dist server/dist/frontend"
	}
}
